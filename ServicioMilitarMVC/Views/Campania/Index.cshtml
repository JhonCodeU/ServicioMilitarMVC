@model IEnumerable<ServicioMilitarMVC.Models.CAMPANIA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.actividad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.actividad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.numeroCampania }) |
            @Html.ActionLink("Details", "Details", new { id=item.numeroCampania }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.numeroCampania })
        </td>
    </tr>
}

</table>
